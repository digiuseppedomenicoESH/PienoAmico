import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary     = Color(0xFF1B6CA8);
  static const primaryDark = Color(0xFF0D4F80);
  static const accent      = Color(0xFFFF8C00);

  // Prezzi
  static const prezzoTop    = Color(0xFF2E7D32); // verde — prezzo più basso
  static const prezzoMid    = Color(0xFFF9A825); // giallo — nella media
  static const prezzoHigh   = Color(0xFFC62828); // rosso — sopra la media

  // UI
  static const background   = Color(0xFFF5F5F5);
  static const surface      = Color(0xFFFFFFFF);
  static const textPrimary  = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const divider      = Color(0xFFE0E0E0);

  // Badge self/servito
  static const selfBadge    = Color(0xFF1565C0);
  static const servitoBackground = Color(0xFFE3F2FD);
}
